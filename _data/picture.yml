# _data/picture.yml
# https://rbuchberger.github.io/jekyll_picture_tag/users/presets/
presets:
  
  ri-mobile:
    markup: direct_url
    fallback_format: webp
    fallback_width: 400
  
  ri-middle:
    markup: direct_url
    fallback_format: webp
    fallback_width: 600
  
  ri-full:
    markup: direct_url
    fallback_format: webp
    fallback_width: 740
  
  ri-fallback:
    markup: direct_url
    fallback_format: original
    fallback_width: 740

  mobile:
    markup: direct_url
    fallback_format: avif
    fallback_width: 400
  
  middle:
    markup: direct_url
    fallback_format: avif
    fallback_width: 600
  
  full:
    markup: direct_url
    fallback_format: avif
    fallback_width: 740
  
  fallback:
    markup: direct_url
    fallback_format: webp
    fallback_width: 740
